# ASL Alphabet Recognition System - Raspberry Pi 4 Requirements
# Optimized versions for ARM architecture

# Core computer vision and ML libraries (Pi optimized versions)
opencv-python==4.5.5.64
mediapipe==0.8.11
numpy==1.21.6

# Text-to-speech (Pi compatible)
pyttsx3==2.90

# System dependencies (install via apt)
# libhdf5-dev libhdf5-serial-dev libhdf5-103
# libqtgui4 libqtwebkit4 libqt4-test python3-pyqt5
# libatlas-base-dev libjasper-dev
# libavcodec-dev libavformat-dev libswscale-dev
# libv4l-dev libxvidcore-dev libx264-dev
# libgtk-3-dev libtbb2 libtbb-dev libdc1394-22-dev
# espeak espeak-data libespeak1 libespeak-dev

# Optional: Performance monitoring
# psutil==5.9.0
# gpiozero==1.6.2  # For Pi GPIO access if needed
